# INFO 5940 — course environment
#
# Every pin has an upper bound on purpose: a student who creates a Codespace
# in week 12 should get the same stack as one who started in week 1.
#
# LangChain is deliberately held at 0.2.x because the assignment notebooks are
# written against that API. langchain-openai 0.1.x requires openai<2, which is
# why openai is capped below 2 as well. Revisit both together, not separately.

# --- LLM stack ---
openai~=1.109
tiktoken~=0.7.0
litellm~=1.73

langchain~=0.2.17
langchain-core~=0.2.43
langchain-community~=0.2.19
langchain-openai~=0.1.25

# --- Data / IO ---
pandas~=2.3
pyarrow~=21.0
openpyxl~=3.1
beautifulsoup4~=4.13
fsspec~=2025.9
pydantic~=2.9

# --- App / notebook ---
streamlit~=1.50
notebook~=7.4
ipykernel~=6.30
python-dotenv~=1.1
